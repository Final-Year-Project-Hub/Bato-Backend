# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# Server Configuration
PORT=4000
NODE_ENV=development

# Better Auth Configuration
BETTER_AUTH_URL="http://localhost:4000"
BETTER_AUTH_SECRET="your-secret-key-here-change-in-production"

# JWT Secrets (Generate strong random strings for production)
# SECURITY WARNING: Never commit actual secrets to version control
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ACCESS_TOKEN_SECRET="your-access-token-secret-here"
REFRESH_TOKEN_SECRET="your-refresh-token-secret-here"

# Gmail OAuth2 Configuration (for OTP emails)
GMAIL_USER="your-email@gmail.com"
GMAIL_CLIENT_ID="your-google-client-id"
GMAIL_CLIENT_SECRET="your-google-client-secret"
GMAIL_REFRESH_TOKEN="your-google-refresh-token"

# FastAPI Backend URL (for AI/RAG features)
FASTAPI_URL="https://bato-ai.onrender.com"
# REQUIRED: API key for FastAPI authentication
FASTAPI_API_KEY="your-fastapi-api-key-here"

# CORS Allowed Origins (comma-separated)
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:5173"
